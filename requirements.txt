# ==========================================================================
# IMPORTANTE: usa Python 3.12 (recomendado). Python 3.13/3.14 aún no tienen
# ruedas (wheels) precompiladas de varias librerías y pip intenta compilarlas
# desde cero, lo que falla si no tienes Rust + Visual Studio Build Tools.
# ==========================================================================
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.31
# psycopg2-binary SÓLO es necesario para PostgreSQL (producción/Docker).
# En local se usa SQLite, así que está comentado para evitar errores de
# compilación. Si vas a usar PostgreSQL, descoméntalo:
# psycopg2-binary==2.9.9
httpx==0.27.0
requests==2.32.3
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-multipart==0.0.9
email-validator==2.2.0
pydantic[email]==2.7.1
# SDK nuevo de Google (reemplaza google-generativeai legacy, EOL nov 2025)
google-genai>=2.10.0
